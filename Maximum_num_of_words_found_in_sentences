class Solution:
    def mostWordsFound(self, sentences: List[str]) -> int:
        max_word = 0
        for sen in sentences:
            words = sen.split()
            num_word = len(words)
            max_word = max(max_word, num_word)
        return max_word
